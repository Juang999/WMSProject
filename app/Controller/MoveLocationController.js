// package & helper
const {Auth} = require('../../helper/helper')

// models
const {
    InvcdDet, 
    MvSublocMstr, MvSublocdDet
} = require('../../models')

class MoveLocationController {
    
}

module.exports = new MoveLocationController()